<UserControl x:Class="FydelisTech_Attack.WPF.Views.SubdomainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <TextBlock Grid.Row="0" Text="🌐 Subdomínios Descobertos" 
                   Foreground="{StaticResource AccentCyanBrush}"
                   FontSize="20" FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Barra de resumo -->
        <Border Grid.Row="1" 
                Background="{StaticResource BgCardBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Foreground="{StaticResource TextSecondaryBrush}">
                    <Run Text="Total:"/>
                    <Run x:Name="TotalCount" Text="0" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                </TextBlock>

                <TextBlock Grid.Column="1" Foreground="{StaticResource TextSecondaryBrush}" Margin="20,0,0,0">
                    <Run Text="Vivos:"/>
                    <Run x:Name="AliveCount" Text="0" FontWeight="Bold" Foreground="{StaticResource AccentGreenBrush}"/>
                </TextBlock>

                <TextBlock Grid.Column="2" Foreground="{StaticResource TextSecondaryBrush}" Margin="20,0,0,0">
                    <Run Text="Mortos:"/>
                    <Run x:Name="DeadCount" Text="0" FontWeight="Bold" Foreground="{StaticResource AccentRedBrush}"/>
                </TextBlock>

                <TextBlock Grid.Column="4" Foreground="{StaticResource TextSecondaryBrush}">
                    <Run Text="Filtro:"/>
                </TextBlock>
                <ComboBox Grid.Column="4" x:Name="FilterCombo"
                          Background="{StaticResource BgMediumBrush}"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          BorderBrush="{StaticResource BorderBrush}"
                          Margin="5,0,0,0"
                          Width="120"
                          SelectionChanged="Filter_Changed">
                    <ComboBoxItem Content="Todos" IsSelected="True"/>
                    <ComboBoxItem Content="Vivos"/>
                    <ComboBoxItem Content="Mortos"/>
                    <ComboBoxItem Content="Com HTTP"/>
                </ComboBox>
            </Grid>
        </Border>

        <!-- Tabela de subdomínios -->
        <DataGrid Grid.Row="2" 
                  x:Name="SubdomainGrid"
                  Background="{StaticResource BgMediumBrush}"
                  Foreground="{StaticResource TextPrimaryBrush}"
                  BorderBrush="{StaticResource BorderBrush}"
                  RowBackground="{StaticResource BgMediumBrush}"
                  AlternatingRowBackground="{StaticResource BgCardBrush}"
                  HeadersVisibility="Column"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  FontSize="12"
                  GridLinesVisibility="Horizontal"
                  VerticalGridLinesBrush="{StaticResource BorderBrush}"
                  HorizontalGridLinesBrush="{StaticResource BorderLightBrush}"
                  SelectionMode="Single">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource BgDarkBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource AccentCyanBrush}" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="11" />
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                    <Setter Property="Height" Value="32" />
                    <Setter Property="Padding" Value="8,0" />
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="8,4" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{StaticResource BgMediumBrush}" />
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background" Value="{StaticResource BgCardBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource BgLightBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Hostname" 
                                    Binding="{Binding Hostname}" 
                                    Width="3*"
                                    FontFamily="Cascadia Code, Consolas"/>
                <DataGridTextColumn Header="IP" 
                                    Binding="{Binding Ip}" 
                                    Width="1.5*"
                                    FontFamily="Cascadia Code, Consolas"/>
                <DataGridTemplateColumn Header="Status" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsAlive, Converter={StaticResource BoolToStatusConverter}}"
                                       Foreground="{Binding IsAlive, Converter={StaticResource BoolToStatusColorConverter}}"
                                       FontWeight="SemiBold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="HTTP" 
                                    Binding="{Binding HttpStatus}" 
                                    Width="70"/>
                <DataGridTextColumn Header="Título" 
                                    Binding="{Binding Title}" 
                                    Width="3*"/>
                <DataGridTextColumn Header="Tecnologia" 
                                    Binding="{Binding Technology}" 
                                    Width="2*"/>
                <DataGridTextColumn Header="Tempo (ms)" 
                                    Binding="{Binding ResponseTimeMs}" 
                                    Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>