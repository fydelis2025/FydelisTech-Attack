<UserControl x:Class="FydelisTech_Attack.WPF.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <TextBlock Grid.Row="0" Text="📋 Relatórios" 
                   Foreground="{StaticResource AccentCyanBrush}"
                   FontSize="20" FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <!-- Info do scan atual -->
        <Border Grid.Row="1" 
                Background="{StaticResource BgCardBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="15" Margin="0,0,0,15">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Foreground="{StaticResource TextSecondaryBrush}">
                        <Run Text="Scan atual:"/>
                        <Run x:Name="DomainLabel" Text="Nenhum" FontWeight="Bold" Foreground="{StaticResource AccentGreenBrush}"/>
                    </TextBlock>
                    <TextBlock x:Name="ScanDateLabel" 
                               Text="" 
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="11"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📄 Exportar JSON" 
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"
                            Click="ExportJson_Click"/>
                    <Button Content="📄 Exportar HTML" 
                            Style="{StaticResource PrimaryButton}"
                            Click="ExportHtml_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Pré-visualização do relatório (texto) -->
        <Border Grid.Row="2" 
                Background="{StaticResource BgDarkBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                               Text="Pré-visualização" 
                               Foreground="{StaticResource AccentGreenBrush}"
                               FontSize="14" FontWeight="SemiBold"
                               Margin="0,0,0,8"/>

                    <Button Grid.Column="1" 
                            Content="Copiar"
                            Style="{StaticResource SecondaryButton}"
                            FontSize="11"
                            Padding="8,4"
                            Height="28"
                            Click="CopyReport_Click"/>
                </Grid>

                <TextBox Grid.Row="1" 
                         x:Name="ReportPreview"
                         Background="Transparent"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         BorderThickness="0"
                         FontFamily="Cascadia Code, Consolas, monospace"
                         FontSize="12"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <!-- Últimos relatórios exportados -->
        <Border Grid.Row="3"
                Background="{StaticResource BgCardBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Foreground="{StaticResource TextSecondaryBrush}">
                    <Run Text="Último exportado:"/>
                    <Run x:Name="LastExportPath" Text="Nenhum"/>
                </TextBlock>

                <Button Grid.Column="1" 
                        Content="📂 Abrir Pasta"
                        Style="{StaticResource SecondaryButton}"
                        FontSize="11"
                        Padding="10,4"
                        Height="28"
                        Click="OpenFolder_Click"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>